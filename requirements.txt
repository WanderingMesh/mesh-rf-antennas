# SPLAT! RF Coverage Mapper — Python dependencies
# GDAL is also required by the `elevation` library for terrain downloads:
#   conda install -c conda-forge gdal   (or: brew install gdal)

# Core scientific computing
numpy>=1.21.0,<2.0.0
pandas>=1.3.0,<2.0.0
scipy>=1.11.0

# Geospatial processing and DEM handling
rasterio>=1.2.0
elevation>=1.1.0

# Image processing and visualization
matplotlib>=3.4.0
scikit-image>=0.18.0
Pillow>=8.0.0

# Web framework
fastapi>=0.68.0
uvicorn[standard]>=0.15.0
jinja2>=3.0.0
python-multipart>=0.0.5
pydantic>=1.8.0

# Pre-computation script (precompute_nevada.py)
tqdm>=4.60.0

# Development dependencies
pytest>=6.0.0
black>=21.0.0
flake8>=3.9.0
