# Longley-Rice RF Coverage Web Application
# Production-ready web application for RF coverage mapping

# Core scientific computing
# NOTE: Aligned on the numpy 2.x ABI. The GDAL CLI/bindings (conda-forge, built
# "np2") and the rest of the compiled geospatial stack (rasterio, shapely,
# scipy, geopandas) are all numpy-2 builds, so numpy must stay >=2. pandas <2.0
# is NOT ABI-compatible with numpy 2 (raises "numpy.dtype size changed"), so
# pandas is bumped to >=2.2.2 (first release with official numpy 2 support).
numpy>=2.0.0,<3.0.0
pandas>=2.2.2,<3.0.0
scipy>=1.11.0

# Geospatial processing and DEM handling
rasterio>=1.2.0
elevation>=1.1.0
geopandas>=0.10.0
shapely>=1.8.0

# Image processing and visualization
matplotlib>=3.4.0
seaborn>=0.11.0
scikit-image>=0.18.0
Pillow>=8.0.0

# Web framework
fastapi>=0.68.0
uvicorn[standard]>=0.15.0
jinja2>=3.0.0
python-multipart>=0.0.5

# Additional utilities
pathlib2>=2.3.0
click>=8.0.0
pydantic>=1.8.0
requests>=2.25.0
aiofiles>=0.8.0
boto3>=1.29.0
scipy>=1.11.0
requests>=2.31.0

# Development dependencies
pytest>=6.0.0
black>=21.0.0
flake8>=3.9.0
